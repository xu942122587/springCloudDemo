server:
  port: 9194
spring:
  application:
    name: sincfin-message, message-server
  cloud:
    config:
      discovery:
        enabled: true
      label: test
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:1111/eureka